version: '3'

services:
  api:
    image: ''
    build:
      context: api
      dockerfile: Dockerfile
    env_file:
      - api/.env.production
    depends_on:
      - db
      - events_db
      - data_db
      - cache
      - search
  queue:
    image: ''
    build:
      context: api
      dockerfile: Dockerfile
    env_file:
      - api/.env.production
    restart: unless-stopped
  web:
    image: ''
    build:
      context: nuxt
      dockerfile: Dockerfile
      args:
        APP_VERSION: local-0.0.1
    networks:
      - nawhas



